import {Avatar} from "@nextui-org/react";

export default function NavBar() {
  return (
    <nav>
      <Avatar
        text="Grad"
        src={""}
        color="gradient"
        textColor="white"
        bordered
        squared
      />
    </nav>
  )
}
